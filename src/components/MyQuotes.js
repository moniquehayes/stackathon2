import React from "react";

const MyQuotes = () => {



};

export default MyQuotes;

//add route to Main, slice to store


//some sort of funcitonlatiy so that when the "add to my quotes" button is clicked, it's added to a separate array in state. then this component will fetch that array. 

